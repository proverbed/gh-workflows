name: Trivial reusable test
on:
  workflow_call:
    inputs:
      issue_number:
        required: false
        type: string
      project_title:
        required: false
        type: string
    secrets:
      GH_PAT:
        required: true
      ANTHROPIC_API_KEY:
        required: true
jobs:
  estimate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "inputs+secrets combined, no permissions, no if"
