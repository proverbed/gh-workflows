name: Trivial reusable test
on:
  workflow_call:
    inputs:
      issue_number:
        required: false
        type: number
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "issue_number=${{ inputs.issue_number }}"
